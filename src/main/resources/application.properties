spring.application.name=SaloonApp

server.port=8081
spring.datasource.url=jdbc:postgresql://localhost:5432/salon_db
spring.datasource.username=postgres
spring.datasource.password=qidtech#1
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.show-sql=true

logging.level.org.springframework.security=DEBUG

# Expose all endpoints (not recommended for prod, use specific ones)
management.endpoints.web.exposure.include=*

# Show detailed health info
management.endpoint.health.show-details=always

info.app.name=Saloon Booking App
info.app.version=1.0.0
